# Default values used the first time the app loads (before Settings is saved).
# Both can also be set at runtime from the Settings panel in the sidebar,
# which persists them to localStorage.

VITE_API_URL=http://localhost:5000
VITE_API_KEY=
